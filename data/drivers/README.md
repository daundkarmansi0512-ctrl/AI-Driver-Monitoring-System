# Driver Profiles

Driver profiles are created locally during application setup.

For privacy reasons, face images, face embeddings, and calibration profiles are not included in the GitHub repository.

Create/register your own driver profile before running the driver recognition features.